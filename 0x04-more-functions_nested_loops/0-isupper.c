#include <stdio.h>
#include <ctype.h>

/**
 * isupper - check whether the character is uppercase or lowercase
 *
 * Return: 1 if uppercase, 0 if lowercase
 */
int main(void)
{
	int _isupper(int c)
	{
		int i;
		i = 65;

		for(i < 91; i++;)
		{
			return (1);
		}
	}
	return (0);
}
